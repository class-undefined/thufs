---
phase: 02-listing-and-transfer-core
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/cli/root.rs
  - src/cli/list.rs
  - src/app/list_service.rs
  - src/output.rs
  - tests/cli_listing.rs
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - CLI-02
autonomous: true
---

<objective>
Implement `thufs ls` on top of the shared repo-resolution and Seafile directory-listing infrastructure.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add list service and root command wiring</name>
  <files>src/cli/root.rs, src/cli/list.rs, src/app/list_service.rs, src/app/mod.rs</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-CONTEXT.md, src/cli/root.rs, src/app/mod.rs</read_first>
  <action>Add a flat `ls` command to the root CLI and back it with a list service that resolves remote refs, fetches remote directory entries, and returns structured results for rendering.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - `src/cli/root.rs` registers an `ls` command
    - list logic is implemented outside the CLI handler
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render deterministic human and JSON listing output</name>
  <files>src/output.rs, src/cli/list.rs</files>
  <read_first>src/output.rs, .planning/phases/02-listing-and-transfer-core/02-CONTEXT.md</read_first>
  <action>Render listing results with clear file-vs-directory distinction in the default human-readable mode and stable structured output in `--json` mode, keeping normal results on stdout and failures on stderr.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - listing output distinguishes files from directories
    - `--json` renders structured listing output
    - tests verify stdout/stderr behavior for successful vs failing list commands
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cover list command behavior with integration tests</name>
  <files>tests/cli_listing.rs, src/app/list_service.rs</files>
  <read_first>tests/cli_contract.rs, src/app/list_service.rs</read_first>
  <action>Add unit and CLI integration tests for `ls` covering explicit repo paths, default-repo shorthand, invalid targets, and script-friendly output behavior.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - there is CLI coverage for `thufs ls`
    - invalid list targets fail without ambiguous success output
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test` succeeds
- [ ] `cargo run -- ls --help` prints successfully
</verification>

<success_criteria>
- `thufs ls` works against remote directories
- Output is deterministic and script-friendly
</success_criteria>
