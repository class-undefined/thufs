---
phase: 02-listing-and-transfer-core
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
files_modified:
  - src/cli/root.rs
  - src/cli/push.rs
  - src/app/push_service.rs
  - src/seafile.rs
  - tests/cli_push.rs
requirements:
  - XFER-01
  - XFER-03
  - XFER-04
  - CLI-02
autonomous: true
---

<objective>
Implement single-file upload with explicit overwrite and path-validation behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add push command and upload service wiring</name>
  <files>src/cli/root.rs, src/cli/push.rs, src/app/push_service.rs, src/app/mod.rs</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-CONTEXT.md, src/cli/root.rs, src/app/mod.rs</read_first>
  <action>Add a flat `push` command that accepts a local source file and remote target, delegates to an upload service, and reuses shared repo/path resolution and output conventions.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - `src/cli/root.rs` registers a `push` command
    - upload behavior is implemented outside the CLI handler
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Seafile upload-link and file-upload flow</name>
  <files>src/seafile.rs, src/app/push_service.rs</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-RESEARCH.md, src/seafile.rs</read_first>
  <action>Implement the Seafile upload flow required for single-file pushes, including upload-link or update-link retrieval, local file validation, and explicit overwrite policy checks.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - push validates the local source file before network upload
    - overwrite behavior is explicit rather than implicit
    - service returns stable success/failure results suitable for stdout/stderr rendering
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Test upload failure and overwrite semantics</name>
  <files>tests/cli_push.rs, src/app/push_service.rs</files>
  <read_first>tests/cli_contract.rs, src/app/push_service.rs</read_first>
  <action>Add unit and CLI integration coverage for upload success, invalid local path, invalid remote target, and overwrite-conflict behavior, with non-zero exit semantics for failure paths.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - CLI coverage exists for `thufs push`
    - overwrite-conflict behavior is tested
    - invalid source or target paths produce deterministic failures
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test` succeeds
- [ ] `cargo run -- push --help` prints successfully
</verification>

<success_criteria>
- `thufs push` uploads a file through the Seafile flow
- overwrite and invalid-path failures are explicit and script-safe
</success_criteria>
