---
phase: 02-listing-and-transfer-core
plan: 04
type: execute
wave: 4
depends_on:
  - "02-01"
files_modified:
  - src/cli/root.rs
  - src/cli/pull.rs
  - src/app/pull_service.rs
  - src/seafile.rs
  - tests/cli_pull.rs
requirements:
  - XFER-02
  - XFER-03
  - XFER-04
  - CLI-02
autonomous: true
---

<objective>
Implement single-file download with safe local-write and explicit failure behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add pull command and download service wiring</name>
  <files>src/cli/root.rs, src/cli/pull.rs, src/app/pull_service.rs, src/app/mod.rs</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-CONTEXT.md, src/cli/root.rs, src/app/mod.rs</read_first>
  <action>Add a flat `pull` command that accepts a remote source file and local destination, delegates to a download service, and preserves the project's output and exit-code contract.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - `src/cli/root.rs` registers a `pull` command
    - download behavior is implemented outside the CLI handler
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Seafile download-link and safe local write flow</name>
  <files>src/seafile.rs, src/app/pull_service.rs</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-RESEARCH.md, src/seafile.rs</read_first>
  <action>Implement the Seafile download flow needed for single-file pulls, including download-link retrieval, streamed file download, temporary-file handling where appropriate, and explicit overwrite checks for the local destination.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - pull does not report success before the final destination file is safely written
    - overwrite behavior is explicit rather than implicit
    - invalid remote or local targets produce deterministic failures
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Test download safety and CLI failure behavior</name>
  <files>tests/cli_pull.rs, src/app/pull_service.rs</files>
  <read_first>tests/cli_contract.rs, src/app/pull_service.rs</read_first>
  <action>Add unit and CLI integration coverage for download success, missing remote file, invalid destination path, and overwrite conflict behavior, ensuring failures return stable non-zero exits and do not leave misleading success output.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - CLI coverage exists for `thufs pull`
    - invalid destination handling is tested
    - failure paths preserve stdout/stderr discipline
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test` succeeds
- [ ] `cargo run -- pull --help` prints successfully
</verification>

<success_criteria>
- `thufs pull` downloads a single file safely
- local-write and overwrite failures are explicit and automation-friendly
</success_criteria>
