---
phase: 02-listing-and-transfer-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/seafile.rs
  - src/config.rs
  - src/contract.rs
  - src/app/mod.rs
requirements:
  - NAV-01
  - XFER-01
  - XFER-02
autonomous: true
---

<objective>
Build the shared Seafile client and repository/path resolution layer that `ls`, `push`, and `pull` all depend on.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Rust HTTP and async dependencies for Seafile operations</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-CONTEXT.md, .planning/phases/02-listing-and-transfer-core/02-RESEARCH.md, Cargo.toml</read_first>
  <action>Add the minimal Rust dependencies needed for Seafile-backed HTTP listing and transfer flows, favoring `reqwest` + `tokio` + existing serde support over bespoke networking.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `reqwest`
    - `Cargo.toml` contains `tokio`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Seafile client primitives and repo lookup</name>
  <files>src/seafile.rs, src/app/mod.rs</files>
  <read_first>.planning/phases/02-listing-and-transfer-core/02-CONTEXT.md, .planning/phases/02-listing-and-transfer-core/02-RESEARCH.md, src/app/mod.rs, src/config.rs</read_first>
  <action>Create a Seafile client boundary that builds authenticated requests from the configured token, models repository and directory entry responses, and exposes deterministic repo lookup by name plus download/upload-link helper methods for later plans.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - `src/seafile.rs` defines a client type for authenticated Seafile requests
    - `src/seafile.rs` defines repository and directory entry models
    - repo lookup logic fails explicitly on missing or ambiguous repository matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Connect canonical remote references to concrete repo resolution</name>
  <files>src/contract.rs, src/config.rs, src/seafile.rs</files>
  <read_first>src/contract.rs, src/config.rs, .planning/phases/01-foundation-and-command-contract/01-03-SUMMARY.md</read_first>
  <action>Extend the remote-path contract as needed so a parsed `RemoteRef` can be resolved into a concrete Seafile repo ID and normalized remote path using either explicit repo syntax or the configured default repo.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - explicit remote refs resolve without requiring default repo config
    - shorthand refs only resolve when a default repo is configured
    - unit tests cover both successful and failing repo-resolution paths
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test` succeeds
- [ ] repo/path resolution tests cover explicit and default-repo forms
</verification>

<success_criteria>
- Shared Seafile client boundary exists
- Repo lookup and remote resolution are reusable by later plans
</success_criteria>
