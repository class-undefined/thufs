---
phase: 01-foundation-and-command-contract
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - internal/contract/remote_ref.go
  - internal/contract/remote_ref_test.go
  - internal/output/render.go
  - internal/output/render_test.go
  - internal/cli/root.go
  - internal/cli/config.go
  - README.md
autonomous: true
requirements:
  - CLI-02
  - CLI-03
user_setup: []
must_haves:
  truths:
    - "Local and remote path handling follows one canonical contract across the CLI."
    - "Explicit fully-qualified remote paths always work even when no default repo is configured."
    - "Normal results go to stdout, errors go to stderr, and structured output is available via `--json`."
  artifacts:
    - path: "internal/contract/remote_ref.go"
      provides: "Canonical remote-path representation and normalization"
      min_lines: 30
    - path: "internal/contract/remote_ref_test.go"
      provides: "Regression tests for explicit and default-repo path handling"
      min_lines: 40
    - path: "internal/output/render_test.go"
      provides: "Output policy regression tests for stdout/stderr and JSON behavior"
      min_lines: 30
    - path: "README.md"
      provides: "Documented Phase 1 command and path contract"
      contains: "Remote path"
  key_links:
    - from: "internal/cli/root.go"
      to: "internal/output/render.go"
      via: "persistent JSON/verbosity flag behavior"
      pattern: "json|verbose|quiet"
    - from: "internal/cli/config.go"
      to: "internal/contract/remote_ref.go"
      via: "default repo integration with canonical remote references"
      pattern: "DefaultRepo|RemoteRef"
---

<objective>
Freeze the shared path and output contracts that all future business commands must obey.

Purpose: Prevent later `ls`, `push`, `pull`, and `share` work from inventing divergent path or rendering behavior.
Output: Canonical remote reference model, output discipline with JSON support, and contract documentation/tests.
</objective>

<execution_context>
@/Volumes/zhitai/code/thufs/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/zhitai/code/thufs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-command-contract/01-CONTEXT.md
@.planning/phases/01-foundation-and-command-contract/01-RESEARCH.md
@.planning/phases/01-foundation-and-command-contract/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-command-contract/01-02-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| CLI process ↔ user input | User-provided local and remote paths enter the contract layer | path strings, flags, output mode |
| CLI process ↔ shell automation | Structured and human-readable outputs are consumed by users or scripts | stdout, stderr, exit codes |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-1-05 | Tampering | remote path normalization | mitigate | canonicalize explicit and default-repo forms into one internal representation with tests | open |
| T-1-06 | Information Disclosure | output contract | mitigate | separate stdout/stderr behavior and ensure JSON output excludes secret-bearing fields unless intentionally exposed | open |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical remote reference normalization</name>
  <files>internal/contract/remote_ref.go, internal/contract/remote_ref_test.go</files>
  <read_first>.planning/phases/01-foundation-and-command-contract/01-CONTEXT.md, .planning/phases/01-foundation-and-command-contract/01-RESEARCH.md, internal/config/config.go</read_first>
  <action>Create `internal/contract/remote_ref.go` with a canonical `RemoteRef` or equivalent type that normalizes explicit fully-qualified remote paths and default-repo shorthand into one internal form. Add tests in `internal/contract/remote_ref_test.go` that prove explicit paths always work without default repo config and that shorthand paths only resolve when a default repo exists.</action>
  <verify>go test ./...</verify>
  <acceptance_criteria>
    - `internal/contract/remote_ref.go` contains a `RemoteRef` type or equivalent canonical remote reference
    - `internal/contract/remote_ref_test.go` contains a test for explicit remote paths without a default repo
    - `internal/contract/remote_ref_test.go` contains a test for shorthand path resolution with a default repo
  </acceptance_criteria>
  <done>Remote path semantics are frozen in code and regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add output policy for human-readable and JSON modes</name>
  <files>internal/output/render.go, internal/output/render_test.go, internal/cli/root.go</files>
  <read_first>internal/output/render.go, internal/cli/root.go, .planning/REQUIREMENTS.md</read_first>
  <action>Extend the renderer and root command plumbing so commands can choose between default human-readable output and `--json`, while preserving stdout for normal results and stderr for errors. Add tests that lock the output mode selection and ensure JSON rendering is routed through the shared renderer rather than ad hoc printing.</action>
  <verify>go test ./...</verify>
  <acceptance_criteria>
    - `internal/cli/root.go` contains a persistent `--json` flag
    - `internal/output/render_test.go` contains a JSON-mode test
    - `internal/output/render_test.go` contains a stdout/stderr behavior test or equivalent verifiable output policy check
  </acceptance_criteria>
  <done>Output discipline is explicit and test-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Document and expose the Phase 1 contract</name>
  <files>README.md, internal/cli/config.go, internal/cli/root.go</files>
  <read_first>README.md, .planning/phases/01-foundation-and-command-contract/01-CONTEXT.md, .planning/PROJECT.md</read_first>
  <action>Update or create `README.md` with the locked Phase 1 command and path contract: token-driven auth, file-first config with env override, explicit remote-path form plus default-repo shorthand, grouped management commands, and output conventions. Where relevant, align help text in `internal/cli/root.go` and `internal/cli/config.go` with the same contract language so docs and CLI help do not drift.</action>
  <verify>go test ./... && test -f README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `Remote path`
    - `README.md` contains `--json`
    - `README.md` contains `token`
    - `README.md` contains `default repo` or `default-repo`
  </acceptance_criteria>
  <done>Phase 1 behavior is documented in both code and operator-facing docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` succeeds
- [ ] explicit remote-path tests pass without relying on a configured default repo
- [ ] JSON-mode and stdout/stderr contract tests pass
</verification>

<success_criteria>

- All tasks completed
- Remote path contract is canonicalized and tested
- Output policy is shared and test-backed
- Documentation reflects the locked Phase 1 contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-command-contract/01-03-SUMMARY.md`
</output>
