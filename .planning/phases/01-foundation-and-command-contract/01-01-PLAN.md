---
phase: 01-foundation-and-command-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/thufs/main.go
  - internal/cli/root.go
  - internal/cli/auth.go
  - internal/cli/config.go
  - internal/app/app.go
  - internal/output/render.go
  - internal/config/config.go
autonomous: true
requirements:
  - CONF-01
  - CLI-01
user_setup: []
must_haves:
  truths:
    - "User can invoke a stable `thufs` root command with grouped `auth` and `config` command trees."
    - "User can discover command structure through built-in help output without seeing unfinished placeholder verbs."
  artifacts:
    - path: "go.mod"
      provides: "Go module foundation for the CLI"
      contains: "module"
    - path: "cmd/thufs/main.go"
      provides: "CLI binary entrypoint"
      contains: "func main()"
    - path: "internal/cli/root.go"
      provides: "Root command and top-level command wiring"
      min_lines: 20
    - path: "internal/cli/auth.go"
      provides: "Grouped auth command scaffold"
      min_lines: 20
    - path: "internal/cli/config.go"
      provides: "Grouped config command scaffold"
      min_lines: 20
  key_links:
    - from: "cmd/thufs/main.go"
      to: "internal/cli/root.go"
      via: "root command construction"
      pattern: "NewRootCommand"
    - from: "internal/cli/root.go"
      to: "internal/cli/auth.go"
      via: "subcommand registration"
      pattern: "NewAuthCommand"
    - from: "internal/cli/root.go"
      to: "internal/cli/config.go"
      via: "subcommand registration"
      pattern: "NewConfigCommand"
---

<objective>
Create the CLI scaffold and command tree that everything else in Phase 1 will build on.

Purpose: Lock the command surface shape early so auth/config behavior and output contracts have a stable home.
Output: Go module bootstrap, root command wiring, grouped management commands, and shared app/output scaffolding.
</objective>

<execution_context>
@/Volumes/zhitai/code/thufs/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/zhitai/code/thufs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-command-contract/01-CONTEXT.md
@.planning/phases/01-foundation-and-command-contract/01-RESEARCH.md
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| CLI process ↔ local terminal | User invokes commands and sees output | command args, stdout, stderr |
| CLI process ↔ local filesystem | App reads/writes config and module files | token, config values, source code |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-1-01 | Tampering | command tree bootstrap | mitigate | keep command registration centralized in `internal/cli/root.go` and cover with help output checks | open |
| T-1-02 | Information Disclosure | help/output layer | mitigate | route normal output through renderer and keep secret-bearing config values out of help/default output | open |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and CLI entrypoint</name>
  <files>go.mod, cmd/thufs/main.go</files>
  <read_first>.planning/phases/01-foundation-and-command-contract/01-CONTEXT.md, .planning/phases/01-foundation-and-command-contract/01-RESEARCH.md</read_first>
  <action>Create `go.mod` for the `thufs` CLI, add the Cobra dependency needed for command wiring, and add `cmd/thufs/main.go` with `func main()` that constructs the root command and executes it with stderr-aware error handling. Keep the entrypoint minimal: build app dependencies, call the root command, and return non-zero exit status on command execution error.</action>
  <verify>test -f go.mod && test -f cmd/thufs/main.go</verify>
  <acceptance_criteria>
    - `go.mod` contains `module` and `github.com/spf13/cobra`
    - `cmd/thufs/main.go` contains `func main()` and `Execute()`
  </acceptance_criteria>
  <done>Go module bootstrap exists and the binary entrypoint is ready to execute the root command.</done>
</task>

<task type="auto">
  <name>Task 2: Build root command and grouped management commands</name>
  <files>internal/cli/root.go, internal/cli/auth.go, internal/cli/config.go, internal/app/app.go, internal/output/render.go</files>
  <read_first>internal/cli/root.go, .planning/phases/01-foundation-and-command-contract/01-CONTEXT.md, .planning/phases/01-foundation-and-command-contract/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create `internal/cli/root.go` with a root `thufs` command that registers grouped `auth` and `config` commands while reserving flat top-level business verbs for later phases. Add `internal/cli/auth.go` and `internal/cli/config.go` to provide scaffold subcommands and help text, create `internal/app/app.go` for shared dependencies, and create `internal/output/render.go` for a central renderer abstraction so later plans do not print directly from service code.</action>
  <verify>go test ./... || true</verify>
  <acceptance_criteria>
    - `internal/cli/root.go` contains `Use: "thufs"` and registers `NewAuthCommand` and `NewConfigCommand`
    - `internal/cli/auth.go` contains `Use: "auth"`
    - `internal/cli/config.go` contains `Use: "config"`
    - `internal/output/render.go` contains a renderer type or interface used for command output
  </acceptance_criteria>
  <done>Command tree shape and shared app/output scaffolding match the Phase 1 command-surface decision.</done>
</task>

<task type="auto">
  <name>Task 3: Lock help and command-discovery behavior</name>
  <files>internal/cli/root.go, internal/cli/auth.go, internal/cli/config.go</files>
  <read_first>internal/cli/root.go, internal/cli/auth.go, internal/cli/config.go, .planning/REQUIREMENTS.md</read_first>
  <action>Refine root and grouped command help text so `push`, `pull`, `ls`, and `share` are clearly reserved as future flat business verbs, while `auth` and `config` are explicitly described as grouped management commands. Ensure no placeholder command descriptions leak token values or imply unsupported login methods.</action>
  <verify>rg -n 'auth|config|push|pull|ls|share' internal/cli/root.go internal/cli/auth.go internal/cli/config.go</verify>
  <acceptance_criteria>
    - `internal/cli/root.go` mentions grouped management commands and future flat business verbs
    - `internal/cli/auth.go` does not contain `password`
    - `internal/cli/config.go` does not describe environment variables as the primary configuration path
  </acceptance_criteria>
  <done>The command surface communicates the intended product model without conflicting with locked Phase 1 decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` runs without hanging
- [ ] `go run ./cmd/thufs --help` prints the root help successfully
- [ ] `go run ./cmd/thufs auth --help` and `go run ./cmd/thufs config --help` print grouped command help
</verification>

<success_criteria>

- All tasks completed
- Root CLI scaffolding exists and runs
- Grouped management commands are discoverable
- No password-login path is implied in the scaffold
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-command-contract/01-01-SUMMARY.md`
</output>
